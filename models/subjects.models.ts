export type Class = {
  id: number;
  number: number;
  datetime_created: string;
  is_deleted: boolean;
};

export type GeneralSubject = {
  id: number;
  name: string;
  datetime_created: string;
  is_deleted: boolean;
}